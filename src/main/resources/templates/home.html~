<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Perfumes</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

<header th:insert="~{fragmentos :: header}"></header>

<section class="perfume-list-container">

    <h2>Perfumes Disponíveis</h2>

    <div class="perfume-list">
        <div th:each="perfume : ${perfumes}" class="perfume-card">
            <img src="/images/perfume.jpg" alt="Perfume" class="perfume-image">
            <div class="perfume-info">
                <h3 th:text="${perfume.nome}">Nome do Perfume</h3>
                <p th:text="'Preço: R$ ' + ${perfume.preco}">Preço</p>
                <p th:text="${perfume.fragrancia}">Fragrância</p>
                <p th:text="${perfume.marca}">Marca</p>
                <p th:text="'Volume: ' + ${perfume.volume} + ' ml'">Volume</p>
            </div>
            <a class="buy-button">Adicionar ao Carrinho</a>
        </div>
    </div>

</section>

<footer th:insert="~{fragmentos :: footer}"></footer>

</body>
</html>